server:
  port: 8080

spring:
  application:
    name: agent-platform
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/agent_platform?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 199726ma.
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 25MB

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    # log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    banner: false

springdoc:
  swagger-ui:
    path: /swagger-ui.html

jwt:
  # HS256 密钥，生产环境务必替换为随机长字符串（>=32字节）
  secret: agent-platform-jwt-secret-change-me-in-production-2026
  expire-hours: 24

logging:
  level:
    com.agent.platform: info
